class NotificationGame {
  DateTime createdAt;
  String gameId;
  String image;
  DateTime gameStartTime;
  String title;

  NotificationGame(
      this.createdAt, this.gameId, this.image, this.gameStartTime, this.title);
}